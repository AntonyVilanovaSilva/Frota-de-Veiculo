@model IEnumerable<Frota_de_Veículos.Models.TipoCombustivel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ValorLitro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ValorLitro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CombId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CombId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CombId })
        </td>
    </tr>
}

</table>
